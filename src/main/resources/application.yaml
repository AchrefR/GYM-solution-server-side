spring:
  application:
    name: demo
  datasource:
    url: jdbc:mysql://localhost:3307/gym_db?useSSL=false&serverTimezone=UTC
    username: root
    password: achref2025

  jpa:
    hibernate:
      ddl-auto: update   # create / update / validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  jackson:
    serialization:
      indent-output: true

server:
  port: 8080