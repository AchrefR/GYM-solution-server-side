# Copy to .env (or export these in your run environment / IDE run config).
# Do NOT commit real secrets.

# --- Database ---
DB_URL=jdbc:mysql://localhost:3307/gym_db?useSSL=false&serverTimezone=UTC
DB_USERNAME=root
DB_PASSWORD=achref2025

# --- JWT ---
# Must be at least 32 characters for HS256.
JWT_SECRET=my-super-secret-key-my-super-secret-key
JWT_EXPIRATION=86400000
